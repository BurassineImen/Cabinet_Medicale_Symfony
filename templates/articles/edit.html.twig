{% extends 'base.html.twig' %}

{% block title %}Modifier Article{% endblock %}

{% block body %}
    <h1>Modifier Article</h1>
    
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}
    
    <a href="{{ path('article_list') }}" class="btn btn-secondary">Retour</a>
{% endblock %}
